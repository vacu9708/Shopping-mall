server.port=8081

# logging.level.root=FATAL

# MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/order_management
spring.datasource.username=root
spring.datasource.password=123
# spring.jpa.hibernate.ddl-auto=update

# Kafka
kafka.host=${KAFKA_HOST:localhost}

product_management.address=${product_management_address:http://localhost:8083}
user_management.address=${user_management_address:http://localhost:8082}